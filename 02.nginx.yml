- name: "Install and run nginx"
  hosts: frontend
  become: true
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start nginx
    ansible.builtin.service:
      name: nginx
      service: started
      enabled: yes
